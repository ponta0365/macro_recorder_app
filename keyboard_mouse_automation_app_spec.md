# キーボード・マウス操作記録アプリ 仕様書

## 1. 目的

PC上のマウス操作とキーボード操作を記録し、あとから再生できるようにする。

スクリーンショット、範囲指定、画像認識は初期版では使わない。

最初に作るものは、単純な操作レコーダー。

人間が実際に操作した流れをそのまま記録し、あとから見やすい名前を付けて整理できるようにする。

## 2. 基本方針

記録開始ボタンを押す。

ユーザーが普段通りにマウスとキーボードを操作する。

アプリは以下を時系列で記録する。

- マウス移動
- 左クリック
- 右クリック
- ダブルクリック
- マウスホイール
- キー入力
- ショートカットキー
- 入力された文字列
- 操作間の待機時間
- 操作時のカーソル座標

記録停止後、操作ログを一覧表示する。

各操作には自動で番号を割り振る。

例。

- 入力欄1
- 入力欄2
- マウスクリック1
- マウスクリック2
- ホイール操作1
- ショートカット1

ユーザーはあとから各操作に意味を付けられる。

例。

- 入力欄1 → 検索キーワード入力
- マウスクリック1 → 検索ボタンを押す
- ホイール操作1 → 一覧を下へスクロール
- マウスクリック2 → 1件目の詳細を開く

## 3. 初期版でやらないこと

初期版では以下は作らない。

- スクリーンショット取り込み
- 画面上への範囲指定
- ボタンや入力欄の画像認識
- OCR
- 複数モニター対応
- 条件分岐
- ループ処理
- AIによる操作判断

まずは、人間の操作を忠実に記録して再生することを優先する。

## 4. 記録する操作

### 4.1 マウス移動

記録する内容。

- 操作種別: mouse_move
- X座標
- Y座標
- 前操作からの経過時間

マウス移動は大量に発生するため、初期版ではクリックやホイール直前の座標を主に使う。

常時すべての移動を細かく保存するとログが膨らむため、圧縮する。

### 4.2 マウスクリック

記録する内容。

- 操作種別: click
- ボタン種別: left / right / middle
- X座標
- Y座標
- クリック回数
- 前操作からの経過時間
- 自動ラベル

自動ラベル例。

- マウスクリック1
- マウスクリック2
- 右クリック1
- ダブルクリック1

ユーザーはあとから表示名を変更できる。

### 4.3 マウスホイール

記録する内容。

- 操作種別: wheel
- X座標
- Y座標
- ホイール量
- 方向: up / down
- 前操作からの経過時間
- 自動ラベル

重要点。

マウスホイールは、カーソルがどこにあるかで動く対象が変わる。

そのため、ホイール操作では必ずカーソル座標を記録する。

再生時は、記録された座標へカーソルを移動してからホイールを動かす。

### 4.4 キー入力

記録する内容。

- 操作種別: key
- キー名
- 押下または解放
- 前操作からの経過時間

Ctrl+C、Ctrl+V、Alt+Tabなどはショートカットとしてまとめて表示できるようにする。

### 4.5 文字入力

記録する内容。

- 操作種別: text_input
- 入力文字列
- 前操作からの経過時間
- 自動ラベル
- 入力モード: ascii / halfwidth / japanese / mixed

入力作業は、人間が流れを追いやすいように自動で入力欄番号を割り振る。

例。

- 入力欄1
- 入力欄2
- 入力欄3

記録中にキーボードから連続して入力された文字は、1文字ずつではなく、ひとまとまりの文字列として扱う。

例。

悪い表示。

- a
- b
- c
- d

良い表示。

- 入力欄1: abcd

Enter、Tab、Esc、Ctrl系ショートカットが入ったら文字入力のまとまりを区切る。
半角と全角が切り替わった場合も、別の入力まとまりとして区切る。
日本語入力や全角文字は、入力モードを付けて扱う。

## 5. 操作ログの表示

記録停止後、操作ログを表で表示する。

表示項目。

| No | 自動ラベル | 表示名 | 操作 | 内容 | 座標 | 待機 |
|---:|---|---|---|---|---|---:|
| 1 | マウスクリック1 | 検索欄を選択 | 左クリック |  | 320, 240 | 0.8 |
| 2 | 入力欄1 | 検索キーワード | 文字入力 | sample |  | 0.2 |
| 3 | マウスクリック2 | 検索ボタン | 左クリック |  | 580, 240 | 0.5 |
| 4 | ホイール操作1 | 結果一覧を下へ移動 | ホイール | down 3 | 900, 620 | 1.0 |

ユーザーが編集できる項目。

- 表示名
- 入力文字列
- 待機時間
- 有効 / 無効
- 削除
- 再生開始前確認
- 座標

自動ラベルは内部識別用として残す。

表示名は人間が意味を追いやすくするための名前。

## 6. 起動設定

自動操作の前に、URLやEXEを起動できるようにする。

起動種別。

- URLを開く
- EXEを起動する
- BAT / CMD / PowerShellを起動する
- フォルダを開く

設定項目。

- 起動名
- 種別
- URLまたはファイルパス
- 起動引数
- 作業フォルダ
- 起動後の待機秒数

例。

```json
{
  "name": "検索ページ",
  "type": "url",
  "target": "https://example.com",
  "wait_seconds": 3
}
```

```json
{
  "name": "業務アプリ",
  "type": "exe",
  "target": "C:\\Program Files\\SampleApp\\SampleApp.exe",
  "args": "",
  "working_dir": "C:\\Program Files\\SampleApp",
  "wait_seconds": 5
}
```

## 7. 再生モード

再生ボタンを押すと、記録された操作を順番に実行する。

再生の流れ。

1. 実行前確認を表示する
2. URLまたはEXEの起動設定があれば実行する
3. 指定秒数待機する
4. 操作ログを上から順番に再生する
5. 各操作の前に記録された待機時間を入れる
6. マウスクリックは記録座標へ移動してクリックする
7. ホイール操作は記録座標へ移動してからホイールを動かす
8. 文字入力は指定文字列を入力する
9. 最後まで終わったら完了ログを出す

## 8. 安全機能

自動操作は誤操作の危険があるため、安全機能を必ず入れる。

必須機能。

- 実行前確認
- 緊急停止キー
- マウスを画面左上へ移動したら停止
- 1操作ごとの実行ログ
- 実行前カウントダウン
- 本実行前のテスト実行

停止キーの扱い。

- `Esc` はグローバル停止キーとして扱う
- アプリにフォーカスがなくても `Esc` で停止できるようにする
- 記録中は `Esc` を押したら記録を終了する
- 再生中も `Esc` を押したら自動操作を即時停止する
- 停止操作として使った `Esc` は、操作ログには記録しない
- `Esc` を単独の入力として記録したい場合は、別の将来仕様として扱う

緊急停止キーの候補。

- Esc長押し
- Ctrl + Alt + Pause
- F12

初期版では、pyautoguiのFailSafeも有効にする。

## 9. 保存形式

プロジェクト単位でJSON保存する。

保存する内容。

- プロジェクト名
- 起動設定
- 記録時の画面解像度
- 操作ログ
- 表示名
- 待機時間
- 有効 / 無効
- 最近使ったプロジェクトの順序

保存例。

```json
{
  "project_name": "sample_operation",
  "screen": {
    "width": 1920,
    "height": 1080
  },
  "launcher": {
    "type": "url",
    "target": "https://example.com",
    "wait_seconds": 3
  },
  "steps": [
    {
      "id": "click_1",
      "auto_label": "マウスクリック1",
      "display_name": "検索欄を選択",
      "action": "click",
      "button": "left",
      "x": 320,
      "y": 240,
      "wait_before": 0.8,
      "enabled": true
    },
    {
      "id": "input_1",
      "auto_label": "入力欄1",
      "display_name": "検索キーワード",
      "action": "text_input",
      "text": "sample",
      "wait_before": 0.2,
      "enabled": true
    },
    {
      "id": "wheel_1",
      "auto_label": "ホイール操作1",
      "display_name": "結果一覧を下へ移動",
      "action": "wheel",
      "x": 900,
      "y": 620,
      "delta": -3,
      "wait_before": 1.0,
      "enabled": true
    }
  ]
}
```

## 10. 画面構成

### 10.1 メイン画面

必要なボタン。

- 記録開始
- 記録停止
- 再生
- テスト再生
- 保存
- 一覧
- 読み込み
- 起動設定

中央に操作ログ一覧を表示する。

右側または下側に、選択中の操作の編集欄を表示する。

プロジェクトは一覧ダイアログから選べるようにし、直近で使ったものを上位に表示する。
操作ログの行をクリックしたら、その操作の編集欄にフォーカスを移してすぐ編集できるようにする。
表の各項目はセル単位で直接編集できるようにし、内容の入力時は操作種別ごとの入力例を表示する。

### 10.2 操作編集欄

編集できる項目。

- 表示名
- 入力文字列
- 待機時間
- 有効 / 無効
- 再生開始前確認

座標は初期版では直接編集できなくてもよい。

必要になったら後から座標編集を追加する。

## 11. 実装候補

初期版はPythonで作る。

候補ライブラリ。

- GUI: tkinter
- マウス・キーボード記録: pynput
- マウス・キーボード再生: pyautogui
- URL起動: webbrowser
- EXE起動: subprocess
- 保存: json

tkinter、pynput、pyautogui、jsonの構成なら軽く作れる。

## 12. 記録処理の考え方

記録中は、pynputでマウスとキーボードのイベントを拾う。

イベントごとに現在時刻を保存し、前イベントとの差分を待機時間として記録する。

キーボード入力は、通常文字が連続した場合にまとめる。

文字入力のまとまりが確定したタイミングで、入力欄番号を割り振る。

例。

```text
クリック
文字入力 abc
Tab
文字入力 def
Enter
```

表示。

```text
マウスクリック1
入力欄1: abc
ショートカット1: Tab
入力欄2: def
ショートカット2: Enter
```

## 13. 初期プロトタイプの完成条件

最初のプロトタイプは以下ができれば完成。

- 記録開始できる
- マウスクリックを記録できる
- マウスホイールを座標つきで記録できる
- 文字入力を入力欄1、入力欄2としてまとめて記録できる
- 操作ログを一覧表示できる
- 表示名をあとから編集できる
- JSON保存できる
- JSON読み込みできる
- 記録した操作を再生できる
- URLまたはEXEを起動してから再生できる
- 緊急停止できる

## 14. 実装チェックリスト

### 記録

- [ ] 記録開始ボタンを作る
- [ ] 記録停止ボタンを作る
- [ ] 左クリックを記録する
- [ ] 右クリックを記録する
- [ ] ダブルクリックを記録する
- [ ] ホイール操作を座標つきで記録する
- [ ] キー入力を記録する
- [ ] 文字入力をひとまとまりにする
- [ ] 入力欄1、入力欄2の番号を自動付与する
- [ ] マウスクリック1、マウスクリック2の番号を自動付与する

### 編集

- [ ] 操作ログ一覧を表示する
- [ ] 表示名を編集できる
- [ ] 入力文字列を編集できる
- [ ] 待機時間を編集できる
- [ ] 操作を無効化できる
- [ ] 操作を削除できる

### 再生

- [ ] クリックを再生する
- [ ] ホイールを座標移動後に再生する
- [ ] 文字入力を再生する
- [ ] キー入力を再生する
- [ ] 待機時間を反映する
- [ ] テスト再生を作る

### 起動

- [ ] URL起動を実装する
- [ ] EXE起動を実装する
- [ ] 起動後待機を実装する

### 保存

- [ ] JSON保存を実装する
- [ ] JSON読み込みを実装する
- [ ] 実行ログを保存する

### 安全

- [ ] 実行前確認を入れる
- [ ] カウントダウンを入れる
- [ ] 緊急停止キーを入れる
- [ ] マウス左上移動で停止する

## 15. 開発順序

1. 記録開始と記録停止
2. マウスクリック記録
3. ホイール記録
4. キーボード記録
5. 文字入力のまとまり化
6. 操作ログ一覧
7. 表示名編集
8. JSON保存と読み込み
9. 再生
10. URL / EXE起動
11. 緊急停止

この順番なら、最初の段階から動くものを確認しながら作れる。

## 16. 実装時の解釈ルール

類似OSSの実装傾向を踏まえて、曖昧だった点を以下のように固定する。

- `Esc` はグローバル停止キーとして扱う
- 記録中の `Esc` は記録終了専用にし、操作ログには残さない
- 再生中の `Esc` は即時停止専用にし、操作ログには残さない
- プロジェクト再生のショートカットは `Ctrl + Alt + 1` 〜 `Ctrl + Alt + 9` とし、各プロジェクトに固定割り当てする
- 画面上にショートカット一覧を表示し、どのプロジェクトがどのキーに対応するかを見えるようにする
- ショートカット一覧では、行をドラッグして割り当てを変更できる
- 9個を超えるプロジェクトは未割当として扱い、一覧で明示する
- 実行フローは、複数プロジェクトの連結、条件分岐、ループを扱える別ダイアログで編集する
- 条件分岐は、クリップボードの文字列または固定文字列を対象に比較する
- 条件の判定は `contains` / `equals` / `startswith` / `endswith` を使う
- 実行フローはツリー構造で編集し、条件ノードは `Then / Else` を持てる
- ループノードは `Body` を持ち、`count` 方式と `while` 方式を切り替えられる
- フロー編集画面にはテンプレート作成を用意する
- ショートカットは、`Ctrl` / `Alt` / `Shift` / `Win` などの修飾キーと通常キーの組み合わせを1件として扱う
- ショートカット表示は `Ctrl + C` のように人間が読みやすい形にする
- ショートカット判定の時間しきい値は設定可能にし、初期値は `1.0` 秒程度を想定する
- ショートカットはキー押下で成立させ、最後のキー押下時に1件として確定する
- 文字入力は、連続した printable な文字を1まとまりの `text_input` として保存する
- `Enter` / `Tab` / `Esc` / `Backspace` / `Delete` / 矢印キー / 修飾キーの押下・解放で、文字入力のまとまりを区切る
- 日本語入力の確定文字列は、確定単位で `text_input` として扱う
- `Backspace` は文字入力中なら削除として扱い、入力列の内容に反映する
- `Space` は文字入力の一部として扱う
- 文字入力の抽出は、`get_typed_strings` 相当の考え方を採用し、非文字キーで区切る
- マウスクリックは、単発クリックとダブルクリックを区別して保存する
- ダブルクリック判定は、設定可能な時間しきい値でまとめる
- マウスホイールは、座標つきで保存する
- ホイール方向は、`delta` の符号で表現する
- 将来的に横スクロールを扱う場合は、`axis` を追加できる JSON 形式にしておく
- 操作間の待機時間は、直前の有効な記録ステップとの差分を使う
- 先頭ステップの待機時間は `0` を基本値にする
- 再生時は、記録時と解像度や DPI が大きく違う場合に警告を出す
- 初期版では、座標の自動補正はしない
- 再生中に例外が起きたら、その時点で停止してエラーを表示する
- テスト再生は、実行前に短く確認できる再生モードとして用意する
- クリックやホイールの再生は、対象座標へカーソル移動後に実行する
- 再生中の座標操作は、`pyautogui` の fail-safe により左上移動でも停止できる
- 記録はグローバルフック前提で、アプリ非フォーカス時でも取得できる
- Windows では通常の利用を前提とし、Linux は将来の拡張対象として扱う

## 17. 参考にしたOSSの傾向

以下のOSSの公開READMEやAPI仕様を参考に、上の解釈ルールを決めた。

- `[boppreh/keyboard](https://github.com/boppreh/keyboard)` は、グローバルフック、`record(until='esc')`、`add_hotkey(..., timeout=1)`、`get_typed_strings()` を持つ
- `[maCrow](https://github.com/biohacker0/maCrow)` は、`Ctrl + C` で記録停止、`double_click_threshold` 調整、`JSON` 保存を持つ
- `[PyMacroRecord](https://github.com/LOUDO56/PyMacroRecord)` は、グローバル系ホットキー、`JSON` 保存、録画/再生停止の分離を持つ
- `[MousePlayback](https://github.com/Stefangansevles/MousePlayback)` は、開始停止ホットキー、キー組み合わせ、輸出入を持つ
- `[Irec / Input-Recorder](https://github.com/Zuzu-Typ/Input-Recorder)` は、縦横ホイール、編集可能な delay、`JSON` 保存を持つ
- `[CrossMacro](https://github.com/alper-han/CrossMacro)` は、グローバルホットキー、テキスト入力、再生の pause/resume を持つ
- `[PyAutoGUI docs](https://pyautogui.readthedocs.io/en/latest/index.html?highlight=failsafe)` は、コーナー移動で fail-safe 停止できる前提を示している

## 18. 保存JSONの具体構造

保存ファイルは1つのプロジェクトを表し、記録内容・再生設定・編集可能な状態をまとめて持つ。

```json
{
  "schema_version": 1,
  "project_name": "sample_operation",
  "project_hotkey": "ctrl+alt+1",
  "created_at": "2026-06-14T09:00:00+09:00",
  "updated_at": "2026-06-14T09:30:00+09:00",
  "screen": {
    "width": 1920,
    "height": 1080,
    "dpi_scale": 1.0
  },
  "launcher": {
    "type": "url",
    "target": "https://example.com",
    "args": "",
    "working_dir": "",
    "wait_seconds": 3.0,
    "enabled": true
  },
  "workflow_enabled": false,
  "workflow": [],
  "settings": {
    "shortcut_timeout_seconds": 1.0,
    "double_click_threshold_seconds": 0.4,
    "test_playback_step_limit": 5,
    "countdown_seconds": 3,
    "stop_key": "esc",
    "record_mouse_move": false
  },
  "steps": [
    {
      "id": "click_1",
      "order": 1,
      "auto_label": "マウスクリック1",
      "display_name": "検索欄を選択",
      "action": "click",
      "button": "left",
      "click_count": 1,
      "x": 320,
      "y": 240,
      "wait_before": 0.8,
      "enabled": true,
      "confirm_before_playback": false
    },
    {
      "id": "input_1",
      "order": 2,
      "auto_label": "入力欄1",
      "display_name": "検索キーワード",
      "action": "text_input",
      "text": "sample",
      "wait_before": 0.2,
      "enabled": true,
      "confirm_before_playback": false
    }
  ],
  "execution_log": []
}
```

各項目の意味。

- `schema_version`: ファイル形式の互換管理用
- `project_name`: プロジェクト識別名
- `project_hotkey`: プロジェクト再生用の固定ショートカット
- `created_at` / `updated_at`: 保存日時
- `screen`: 記録時の画面情報
- `launcher`: 起動設定
- `workflow_enabled`: 実行フローを使うかどうか
- `workflow`: プロジェクト連結・条件・ループの実行ノード
- `settings`: 再生や記録に共通する設定
  - `confirm_before_playback`: 再生開始時の確認を出すかどうか
- `steps`: 操作ログ本体
  - `execution_log`: 再生時の実行結果ログ
  - `record_mouse_move`: マウス移動記録の有無
- `project_hotkey` が空なら、そのプロジェクトは未割当として扱う
- `Ctrl + Alt + 1..9` の9枠を超える分は、初期状態では未割当として残す

`steps` の共通項目。

- `id`: 内部ID
- `order`: 表示順
- `auto_label`: 自動生成ラベル
- `display_name`: ユーザーが編集する表示名
- `action`: `click` / `wheel` / `key` / `text_input` / `move`
- `wait_before`: この操作の前に待つ秒数
- `enabled`: 再生対象かどうか
  - `confirm_before_playback`: 個別ステップの実行時確認

操作別の追加項目。

- `click`: `button`, `click_count`, `x`, `y`
- `wheel`: `delta`, `direction`, `axis`, `x`, `y`
- `key`: `key_name`, `key_event`, `modifiers`
- `text_input`: `text`
- `text_input`: `text`, `input_mode`
- `move`: `x`, `y`

`workflow` の各ノードは次のいずれかとする。

- `project`
  - `name`
  - `project_path`
  - `enabled`
- `condition`
  - `name`
  - `source`: `clipboard` / `text` / `file`
  - `operator`: `contains` / `not_contains` / `equals` / `not_equals` / `startswith` / `endswith` / `regex` / `is_empty` / `not_empty`
  - `text`: `source=text` のときの判定元文字列
  - `file_path`: `source=file` のときの対象ファイルパス
  - `file_encoding`: `source=file` のときの文字コード
  - `value`: 比較値
  - `then`: 真のときに実行するノード配列
  - `elif`: 追加条件ノード配列
  - `else`: 偽のときに実行するノード配列
  - `enabled`
- `loop`
  - `name`
  - `mode`: `count` または `while`
  - `repeat_count`: `count` のときの繰り返し回数
  - `source`: `while` 条件の対象 (`clipboard` / `text` / `file`)
  - `operator`: `while` 条件の判定 (`contains` / `not_contains` / `equals` / `not_equals` / `startswith` / `endswith` / `regex` / `is_empty` / `not_empty`)
  - `text`: `source=text` のときの判定元文字列
  - `file_path`: `source=file` のときの対象ファイルパス
  - `file_encoding`: `source=file` のときの文字コード
  - `value`: 比較値
  - `body`: ループ本体のノード配列
  - `enabled`

`execution_log` は再生結果専用とし、少なくとも以下を保存できるようにする。

- 実行時刻
- 対象ステップID
- 成功 / 失敗
- エラーメッセージ
- 停止理由

## 19. 記録イベントから保存ステップへの変換

記録中の生イベントは、そのまま保存せず、再生しやすいステップに正規化する。

- マウス移動記録は任意で、既定ではオフにする
- オフのときは移動イベントを保存せず、前後の差分は `wait_before` に吸収する
- オンのときだけ、必要なら直前座標だけを残し、細かい軌跡は保存しない
- 左/右/中クリックは `click` に変換する
- ダブルクリックは `click_count = 2` の `click` に変換する
- ホイールは `wheel` に変換する
- 修飾キー + 通常キーは `key` または `shortcut` 相当としてまとめる
- 連続文字入力は `text_input` に変換する
- 日本語や全角文字を含む入力は `input_mode` を付けて保存する
- 記録停止に使った `Esc` は保存しない

この変換後の `steps` を、再生と編集の基準データにする。

## 20. 再生フローの固定

再生は次の順で行う。

1. 保存データを読み込む
2. `enabled=false` のステップを除外する
3. 実行前確認を出す
4. `launcher.enabled=true` なら起動する
5. `launcher.wait_seconds` を待つ
6. `settings.countdown_seconds` 分カウントダウンする
7. `steps` を `order` 順に実行する
8. 各ステップ前に `wait_before` を待つ
9. 途中で `Esc` または fail-safe が発火したら即停止する
10. 最後まで成功したら完了ログを記録する

`workflow_enabled=true` の場合は、`steps` ではなく `workflow` を再帰的に実行する。`workflow` 内でも `enabled=false` のノードはスキップする。

## 21. 初期版の非対応事項

初期版では、以下は明示的に対応しない。

- 座標の自動補正
- 画像認識
- OCR
- 複数モニター前提の自動補正
